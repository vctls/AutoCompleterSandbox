[{% for author in authors -%}
    {{ {id: author.id, text: author.__toString}|json_encode|raw }}
    {# use the following for jquery-ui 
    {{ {value: author.id, label: author.__toString}|json_encode|raw }}
    #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]
